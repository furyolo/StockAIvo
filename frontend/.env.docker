# Docker Compose maps host.docker.internal to the host gateway via
# extra_hosts: "host.docker.internal:host-gateway". If your Docker engine
# does not support host-gateway, replace this value with your host machine IP.
VITE_OPENAI_API_BASE=http://host.docker.internal:3222/v1
