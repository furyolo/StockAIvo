# 新闻情感分析缓存任务规划

> 参考 `docs/task-history/2025-10-technical-analysis-cache.md` 的落地经验，面向新闻情感分析 Agent 制定缓存方案。待实现完毕后将本文件归档至 `docs/task-history/`，命名格式 `YYYY-MM-news-sentiment-cache.md`。

## 背景
- [x] 新闻原始数据仅保存在 Redis，Agent 每次执行都会重新聚合全文情感，缺乏结果缓存。
- [x] 现有缓存 TTL 固定 30 分钟：热点股票在盘中更新频繁，分析结果会滞后；普通股票新闻稀疏，重复分析浪费 LLM 资源。
- [x] 缺少与新闻更新事件绑定的失效策略，Redis 异常时也未明确降级路径。

## 目标
1. 为新闻情感分析结果建立“热点分级 + 事件驱动”的缓存策略，兼顾实时性与性能。
2. 保障 Redis 不可用或新闻更新时可自动降级或强制失效，主流程不中断。
3. 为后续监控提供基础数据（命中率、热点触发次数等），便于持续调优。

## 关键约束
- 缓存键命名：
  - 结果键 `news_sentiment:{ticker}`（Value 内含 `latest_publish_time`、结果文本、指标等）。
  - 摘要键 `news_sentiment:summary:{ticker}`，记录 `latest_publish_time`、热点等级、TTL、生成时间等元数据。
- 缓存字段：分析文本、最新新闻时间戳、生成时间、Agent 版本、新闻条数、热点等级。
- 热点分级规则：
  - 最近 2 小时新闻条数 ≥3 → **热点**（热点名单命中可直接升为热点）。
  - 最近 2 小时新闻条数为 1–2 → **关注**。
  - 其他情况 → **普通**。
- TTL 档位：热点 10 分钟、关注 30 分钟、普通 6 小时；但若发现新新闻立即失效，不受 TTL 限制。
- 降级要求：Redis 读取/写入失败时，必须直接调用 Agent 并返回结果，同时记录告警日志。

## 交付物
- [ ] 缓存实现代码及单元测试（覆盖各档 TTL、事件失效、Redis 异常）。
- [ ] 热点分级与 TTL 计算辅助工具/函数。
- [ ] 监控埋点或日志规范，记录命中率与热点触发次数。
- [ ] 文档更新（README / projectBrief / CLAUDE 等）说明策略与调试方法。

## 实施步骤
- [ ] 梳理新闻情感 Agent 的输入输出与 Redis 使用路径，确认所需字段。
- [ ] 在 `CacheManager` 设计结果键与摘要键结构，实现热点分级与 TTL 计算。
- [ ] 在 Agent 调用链执行以下逻辑：
  1. 拉取新闻并获取最新时间戳/条数；
  2. 读取摘要键，判定是否命中、TTL 是否过期；
  3. 发现新闻更新或 TTL 过期 → 触发重新计算并更新缓存与摘要；
  4. Redis 异常 → 降级为直接调用 Agent。
- [ ] 为热点、关注、普通三档编写单元测试，验证 TTL、生效与事件驱动失效。
- [ ] 增加日志/监控指标，记录命中原因（TTL 命中、事件失效、Redis 降级）。
- [ ] 更新开发文档与运维手册，完成后将本文件迁移至 `docs/task-history/` 并按规范命名。

> ✅ 全部勾选后，归档本文件并在任务记录中标注成果与测试结论。
